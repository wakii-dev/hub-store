# FI-280 SF-1 Context Pack — Baseline + Rubric (Tier 0)
> Đọc file này THAY VÌ tự tổng hợp. Epic spec: docs/superpowers/specs/fi280-qa-hub-store-regression-spec.md. Bracket: docs/superpowers/brackets/fi280-qa-hub-store-regression.md.

## Spec slice (SF-1 chịu trách nhiệm)
1. Boot-verify full stack trên main @ d107f2f: `E2E=1 bash scripts/boot-all.sh` — 7/7 ports (web :3000, bff :8080, fulfillment :50051, batching :50052, print :50053, postgres :5432, keycloak :8081) + kafka + realm import thành công.
2. Chạy full Playwright suite hiện có (25 specs, `e2e/tests/01..09`, serial) → baseline report ĐỎ/XANH từng spec (lưu file report + ghi bảng lên issue FI-281).
3. Viết rubric P0–P3 + coverage map 25 specs + file-ownership rules vào `docs/superpowers/qa-rubric.md` (copy từ epic spec — ground truth cho SF-2..7).
4. Fix bug làm baseline đỏ — **CHỈ bug INFRA/seed/boot/fixture** (môi trường làm spec đỏ): docker, seed data, boot script, e2e fixture, port conflict. Bug APP thật (code sai behavior) → KHÔNG fix — log comment lên epic FI-280 theo template `[P<n>][<DOMAIN>]` + routing entry sang SF domain tương ứng.
5. Probe /tmp toolchains còn sống không: ts-proto (/tmp npm dir), grpc-java plugin (/tmp/sf1-spikes) — chết thì rebuild theo memory fi233-story-patterns.

## Touch map (SF-1 sở hữu)
```
docs/superpowers/qa-rubric.md        (mới — rubric + coverage map)
scripts/*                            (chỉ fix nếu boot/seed script bug)
e2e/ fixtures + config               (chỉ fix infra-level)
docker-compose.yml                   (chỉ fix infra-level)
```
READ-ONLY: apps/**, packages/**, services/** code (bug app = route, KHÔNG fix).

## ACCEPTANCE (user-visible — Phase 5 kiểm từng dòng)
1. `E2E=1 bash scripts/boot-all.sh` boot 7/7 ports sạch từ nhánh đích.
2. Baseline report 25/25 specs có verdict đỏ/xanh, ghi trên FI-281.
3. `docs/superpowers/qa-rubric.md` tồn tại chứa rubric + coverage map + ownership rules.
4. Mỗi spec đỏ baseline có: fix-infra commit HOẶC routing entry comment lên epic.
5. Toolchain probe kết quả ghi trên FI-281 (sống/chết/rebuilt).

## Boundary
- KHÔNG fix bug app (route sang SF domain).
- KHÔNG sửa behavior hiện có.
- KHÔNG đụng nhánh main — mọi việc trên worktree fork từ story/qa-hub-store-regression.
- Merge target: story/qa-hub-store-regression. Regression spec range: KHÔNG có (SF-1 không viết spec mới).
