# FI-245 SF-12 Context Pack — Production hardening
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.12. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 4 — CUỐI CÙNG, deps SF-5 + SF-11 Done. Chạy trên toàn bộ diff story.

## Spec slice
1. **M-3 resolved (s2s auth)**: chọn 1 trong 2 — (a) token passthrough: BFF forward access token, fulfillment/batching verify JWKS trực tiếp (giống BFF — jose/JWKS cache pattern); (b) mTLS nội mạng compose (certs qua volume, client-ca verify). CHỌN 1, ghi rationale vào README + comment. `x-user-role` thành derived (verify token xong mới tin role).
2. **Secrets ra khỏi git**: gitignore `.env` + `.env.example` giữ placeholder; rotate TẤT CẢ credentials hiện tracked (postgres password, KC admin secret, realm passwords) — đổi giá trị default mới; compose đọc `${POSTGRES_PASSWORD:?}` từ .env local (file không commit). README hướng dẫn setup .env từ .env.example.
3. **Healthchecks**: endpoint `/health` (mọi service: Java actuator/Go http/BFF) check DB connectivity; compose healthcheck wiring; logs structured JSON (thay console.log nơi dây dưa).
4. **CI/CD**: `.github/workflows/ci.yml` — lint + unit tests (java/go/node) + E2E với E2E=1 (services: postgres + keycloak) + docker build; chạy mỗi PR + push main.
5. **Backup**: `scripts/backup-db.sh` (pg_dump cả 2 DB, gzip, giữ N bản) + cron wiring doc/systemd user + restore doc (README).
6. **Security final audit**: dispatch security-audit trên diff tổng FI-245 (toàn bộ SF-1..12) — P0/P1 phải resolved trước DONE.

## Touch map (SF-12 sở hữu)
```
.github/workflows/ci.yml                   (mới)
scripts/backup-db.sh                       (mới)
.env / .gitignore / .env.example           (secrets)
services/*/                                (health endpoint + token passthrough/mTLS wiring — LIGHT touch, không refactor)
services/bff-gateway/src/plugins/auth.ts   (forward token thay vì derive-only)
docker-compose.yml                         (healthcheck + secrets wiring)
README.md                                  (s2s rationale + backup/restore + secrets setup)
docs/superpowers/…                         (plan file)
```
READ-ONLY ngoài các điểm trên; convergence-fix được phép nhưng audit comment rõ.

## ACCEPTANCE (user-visible)
- Gọi thẳng gRPC :50051/:50052 không có token hợp lệ → bị TỪ CHỐI (trước đây giả role được).
- Clone repo mới (không có .env) → README chỉ ra cách tạo .env → compose up chạy được; git history không còn secrets mặc định mới.
- `curl :8080/health` + services health trả OK/khỏi khỏe DB đúng.
- Push PR → CI chạy xanh (test + E2E + build).
- `bash scripts/backup-db.sh` → file .sql.gz mở được; restore doc theo được.

## Boundary (KHÔNG làm)
- KHÔNG TLS domain thật/HA/k8s/helm/horizontal scaling.
- KHÔNG vault sản phẩm thật (env-file local là đủ mức này).
- KHÔNG refactor nghiệp vụ — chỉ hardening touch.
