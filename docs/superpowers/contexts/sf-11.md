# SF-11 Context Pack — Convergence + QA
> Đọc file này THAY VÌ tự tổng hợp. Spec thực thi: docs/superpowers/specs/ict-service-support-polyglot-spec.md (v3 §5 SF-11). Bracket: docs/superpowers/brackets/fi233-polyglot-grpc-mf.md. Epic: FI-233.
> Tier 5 (deps SF-8, SF-9, SF-10). Mọi SF đã merge — hệ thống đầy đủ trên story branch. Bạn KHÔNG viết feature mới — chỉ verify, tích hợp, audit, đóng gói.

## Spec slice (SF-11 chịu trách nhiệm)
1. **Backend-only integration verify (P0 plan-critic — làm ĐẦU TIÊN)**: boot Java + Go KHÔNG FE — qua gRPC trực tiếp assert: create-batch → **MutateOrderStatus thật** (order batchStatus đổi trong Java), **GetOrdersByCodes hydration** (Go lấy truth validate rule 1), cancel → revert. Bắt mis-wire proto/metadata SỚM (trước E2E).
2. **E2E Playwright 1-2 luồng §8 chính cross-remotes**: đơn (D1, filter 30201) → tick → tạo phiếu (D1b, DnD, gán shipper) → phiếu hiện ở D2 (fulfillment remote) → hủy → đơn revert → tạo lại → In (D3 PDF) → hoàn tất soạn. Boot QUA Playwright webServer config chạy toàn hệ thống (BFF + 3 services + shell + 2 remotes) — KHÔNG boot tay 7 process.
3. **Cross-remote invalidation verify**: tạo phiếu ở orders → navigate sang fulfillment → D2 hiện phiếu mới (cơ chế `refetchOnMount:'always'`).
4. **Role matrix verify 3 roles §2**: Coordinator thấy D1+D2+Print; Ops chỉ D2+Print; Manager tất cả (shell route gating + UI disable).
5. **i18n audit BINARY**: switch VI/EN trên cả 5 screens → **zero missing-key warning** (console i18next); checklist pass/fail post lên Linear comment.
6. **COD/format audit**: `15.000.000đ` (VI) / `15,000,000 ₫` (EN); formatPeriodOfTime nhất quán.
7. **Degraded-mode verify thật**: kill Go process → D1 vẫn render (batchCode trống), mutation batches → 503 `UPSTREAM_UNAVAILABLE` envelope; khôi phục.
8. **Build all + docker-compose cấu hình mẫu**: turbo cache TẮT cho build federation (remoteEntry stale); compose: 4 services + nginx static shell/remotes (publicPath prod theo SPIKE 1 verdict). Đây là cấu hình MẪU chạy được local — KHÔNG prod deploy.
9. **README full**: run toàn hệ thống (1 lệnh dev + compose), port map, env vars, toolchain requirements (java 17, go ≥1.21, python ≥3.11, protoc/buf).
10. **Full §8b regression walkthrough** (D1 9 dòng + D1b 7 + D2 5 + D3 4) — browser Rule 0 3 tầng; kết quả từng dòng post Linear comment.
11. **Final gate + STORY-CLOSE verify**: các check story-workflow (nhánh đích chứa mọi SF, sub-issues Done, worktrees dọn, Epic Done).

## Touch map (SF-11 sở hữu)
```
docker-compose.yml, README.md (root)
e2e/**                          (Playwright specs + webServer config)
scripts/** (system boot/verify helpers nếu cần)
```
READ-ONLY: toàn bộ code feature (SF-1..10). Fix bug tìm thấy = quay lại SF sở hữu (task-executor fix) hoặc fix trực tiếp nếu trivial + ghi audit — KHÔNG refactor vượt scope.

## ACCEPTANCE (user-visible)
- Luồng §8 end-to-end chạy pass trên browser: đơn → phiếu → hủy → revert → in → hoàn tất (video/screenshot bằng chứng).
- 3 roles gate đúng; VI/EN không missing key; COD format đúng 2 locale.
- Kill Go → hệ thống degrade đúng chính sách (không crash).
- `docker-compose up` + README → hệ thống lên đầy đủ cho người mới.
- §8b regression: 25/25 dòng pass (checklist trên Linear).

## Boundary (KHÔNG làm)
- KHÔNG feature mới; KHÔNG sửa kiến trúc; KHÔNG prod deploy/CI-CD (compose mẫu là tối đa).
- KHÔNG merge story branch vào main (người dùng tự làm).
- Bug lớn (kiến trúc/contract gãy) → REQUIREMENT-GAP + flag, KHÔNG tự redesign.
