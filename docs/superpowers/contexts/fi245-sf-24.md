# FI-245 SF-24 Context Pack — Map view
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.24. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 5 — deps SF-16 + SF-20 Done.

## Spec slice
1. Map component dùng chung: **Leaflet + OpenStreetMap tiles** (KHÔNG cần API key; pin version trong package.json).
2. **Batch route view**: từ tracking modal (SF-16) — warehouse marker + stops theo thứ tự `stopOrder` (đánh số + polyline nối thứ tự), popup: đơn, địa chỉ, COD.
3. **Tech service pins** (SF-20): pins đơn giao/lắp đặt theo receiver lat/long, màu theo trạng thái, popup chi tiết + nút gọi.
4. Fallback khi thiếu lat/long: ẩn marker + note "chưa có tọa độ" (KHÔNG geocode hàng loạt).
5. Design system SF-6 (popup styling, nút); KHÔNG đổi testid screens cũ; E2E spec mới (map render + markers theo seed lat/long).

## Touch map (SF-24 sở hữu)
```
packages/shared/src/map/ (mới — Leaflet wrapper)
apps/fulfillment-mf/src/ (map trong tracking modal)
apps/shell/src/ hoặc tech screen (pins view)
e2e/xx-map.spec.ts (mới)
```
READ-ONLY: services/**, compose, apps khác.

## ACCEPTANCE (user-visible)
- Tracking modal có tab/nút bản đồ → thấy warehouse + các stop đánh số đúng thứ tự phiếu.
- Tech service thấy pins đơn theo trạng thái màu; popup có info + gọi.
- Map responsive, không vỡ trong modal.
- E2E cũ + mới xanh.

## Boundary (KHÔNG làm)
- KHÔNG tối ưu lộ trình trên map (suggest đã có — SF cũ).
- KHÔNG Google Maps/API key dịch vụ trả phí.
- KHÔNG geocoding tự động địa chỉ thiếu tọa độ (sau nếu cần).
