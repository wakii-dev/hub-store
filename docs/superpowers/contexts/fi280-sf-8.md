# FI-280 SF-8 Context Pack — Convergence Regression (Tier 2)
> Đọc file này THAY VÌ tự tổng hợp. Epic spec: docs/superpowers/specs/fi280-qa-hub-store-regression-spec.md. Bracket: docs/superpowers/brackets/fi280-qa-hub-store-regression.md.

## Spec slice (SF-8 chịu trách nhiệm)
1. **Merge order: SF-2 TRƯỚC TIÊN** (chủ shared perm files — SF sau rebase lên perm-fix thay vì conflict), rồi SF-3, SF-4, SF-5, SF-6, SF-7 lên nhánh đích story/qa-hub-store-regression. Merge-ngược an toàn: merge PARENT vào sf-branch trước → update-ref FULL refname + 2 ancestor guards (merge-playbook). Conflict improvements-log → giữ CẢ HAI entries.
2. Full e2e suite trên nhánh đích: 25 specs cũ + regression mới 10xx–15xx → báo cáo đỏ/xanh.
3. **Reset-db replay**: `bash scripts/reset-db.sh` + boot lại trên nhánh đích sau merge (chứng minh reset-db không vỡ — table-gap lesson: SF mới thêm bảng runtime → UNIQUE conflict replay silent-fail).
4. Cross-flow sanity: FULL monorepo typecheck + build (semantic conflict thường nổ ở bff routes / formatters import, không chỉ shared) + 02-role-matrix (permission matrix không vỡ chéo).
5. Fix bug `[PERM]` queue: bug permission SF khác log SAU khi SF-2 merge → SF-8 fix (SF-8 kế thừa ownership permission files).
6. **Bug traceability**: bảng đối chiếu bug-log P0–P2 ↔ regression specs — mỗi bug fix có đúng 1 spec trong 10xx–15xx và spec đó PASS. Bug rơi lọt = fail.
7. Consolidate bug-log P3+ → comment cuối lên epic FI-280 (mục lục toàn bộ bug P3/latency đã log).
8. Final browser walkthrough smoke qua nhánh đích (login → orders → fulfillment → D1 → logout).

## Touch map (SF-8 sở hữu)
```
story/qa-hub-store-regression (merge commits)
permission shared files (kế thừa ownership từ SF-2 — chỉ fix bug [PERM] queue)
báo cáo full-suite + bảng traceability (comments trên FI-288)
```

## ACCEPTANCE (user-visible — Phase 5 kiểm từng dòng)
1. Full suite xanh trên nhánh đích (25 cũ + 10xx–15xx).
2. Reset-db replay PASS (boot lại sạch sau reset).
3. Typecheck + build full monorepo PASS.
4. Bảng traceability bug↔spec hoàn chỉnh, 0 bug P0–P2 thiếu spec.
5. Bug-log P3+ consolidated lên epic.
6. Walkthrough smoke PASS qua browser.

## Boundary
- KHÔNG fix bug mới ngoài [PERM] queue + bug suite convergence lộ ra (fix trong SF-8 nếu nhỏ; lớn → escalate epic).
- KHÔNG merge vào main — DỪNG ở nhánh đích (người dùng tự merge).
- Clean up: sau merge tất cả SF → worktree/branch sf-* xóa theo merge-playbook (giữ nhánh đích).
