# FI-280 SF-5 Context Pack — Batching/D1 + Realtime sweep (Tier 1)
> Đọc file này THAY VÌ tự tổng hợp. Epic spec: docs/superpowers/specs/fi280-qa-hub-store-regression-spec.md. Bracket: docs/superpowers/brackets/fi280-qa-hub-store-regression.md.

## Spec slice (SF-5 chịu trách nhiệm)
Tạo batch + DnD grouping + D1 page realtime qua Kafka side-channel + map tracking. KAFKA CHẾT ≠ BUG APP — spec 05-kafka là canary, check canary TRƯỚC khi fix bất kỳ bug realtime.
Walkthrough specs: 05-kafka, 07-realtime, 08-map
Quy trình mỗi flow: Walkthrough = browser thật theo Rule 0 3 tầng (DOM→VISUAL→FLOW), specs hiện có làm CHECKLIST flow; KHÔNG chỉ chạy spec pass. Bug tìm thấy → report comment lên FI-285 theo template `[P<n>][<DOMAIN>] <title> / repro / expected vs actual / evidence / fix commit / regression spec`. Fix P0–P2 ngay; P3 + latency chỉ LOG. >8 bug P2 → STOP + escalate epic.
Sau khi fix: VIẾT regression spec (range 13xx, tự lập state, KHÔNG import/sửa e2e/tests/sf11-helpers.ts) + re-run walkthrough specs domain (verify-no-regression).

## Touch map (SF-5 sở hữu)
services/batching-service + apps/orders/src/batching + D1 page + e2e/tests/13xx-*
CẤM SỬA (ownership SF khác): packages/shared/src/hooks/usePermissions.tsx + nav config (SF-2 duy nhất) + e2e/tests/sf11-helpers.ts. Bug trong file cấm-sửa → log + comment pointer lên issue chủ (SF-2 = FI-282, tag [PERM]). Migration DB: KHÔNG thêm bảng mới; bắt buộc thì cập nhật scripts/reset-db.sh CÙNG commit.

## ACCEPTANCE (user-visible — Phase 5 kiểm từng dòng)
1. Từng walkthrough spec: flow đi trọn qua browser PASS (DOM+VISUAL+FLOW) hoặc bug đã fix.
2. 0 bug P0–P2 mở trong domain (bug-log comment hoàn chỉnh từng bug trên issue).
3. Regression specs range 13xx PASS (tự lập state).
4. verify-no-regression: walkthrough specs domain re-run PASS sau fix.

## Boundary
- KHÔNG sửa shared permission files (SF-2). KHÔNG feature mới. KHÔNG đổi kiến trúc.
- Worktree fork từ story/qa-hub-store-regression; private-port seam E2E_SHELL_URL/E2E_PROXY/E2E_PG_SEAM (pattern run-nvc-private.sh); boot FULL stack riêng port riêng — KHÔNG share Kafka/Keycloak.
- Merge target: story/qa-hub-store-regression (no-ff, conflict improvements-log giữ CẢ HAI).
