# FI-245 SF-22 Context Pack — i18n vi/en toàn app
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.22. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 4 — deps SF-6 Done.

## Spec slice
1. i18n framework nhẹ (i18next hoặc tương đương) + namespace theo module (shell, orders, batching, fulfillment, print, area-staff, d2c, tech, dashboard, users).
2. Bản VI ĐẦY ĐỦ (mặc định, khớp nội dung hiện có 100%) + bản EN toàn bộ screens.
3. Language switcher trong shell (dropdown hoặc icon), persist localStorage, đổi không cần reload.
4. Chuẩn cho code mới từ điểm này: KHÔNG hardcode UI string — i18n keys bắt buộc (ghi convention vào docs ngắn).
5. KHÔNG đổi testid/DOM mà E2E cũ phụ thuộc (text có thể đổi → specs cũ nếu assert text phải chuyển key-agnostic; KHÔNG sửa assertions nghiệp vụ).

## Touch map (SF-22 sở hữu)
```
apps/*/src/  (trích string → keys; KHÔNG đổi logic/DOM)
packages/shared/src/i18n/ (mới — setup)
e2e/xx-i18n.spec.ts (mới)
```
READ-ONLY: services/**, e2e specs cũ (chỉ fix text-assert nếu vỡ, ghi audit).

## ACCEPTANCE (user-visible)
- Switch EN → toàn bộ screens (đã có) hiển thị tiếng Anh hợp lý; switch VI → về như cũ.
- Đổi ngôn ngữ giữ nguyên trang + persist sau reload.
- E2E cũ + mới xanh.

## Boundary (KHÔNG làm)
- KHÔNG ngôn ngữ thứ 3. KHÔNG dịch data nghiệp vụ (tên đơn/trạng thái từ BE — BE trả theo lang sau nếu cần).
- KHÔNG đổi business logic.
