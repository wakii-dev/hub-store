# SF-2 Context Pack — gRPC health + probes code

> Đọc file này THAY VÌ tự tổng hợp từ bracket + epic + comments.
> Epic spec: docs/superpowers/specs/fi24x-minikube-deploy-spec.md · Bracket: docs/superpowers/brackets/fi272-minikube-deploy.md · Epic: FI-272
> Quyết định epic đã chốt (KHÔNG hỏi lại): dùng gRPC health protocol (KHÔNG exec probe) · SF-2 KHÔNG phụ thuộc SF-1 (test standalone).

## Spec slice (SF-2 chịu trách nhiệm)

1. **Java fulfillment-service** (Spring Boot + grpc-server-spring-boot-starter, port 50051):
   implement `grpc.health.v1` qua `HealthStatusManager` — register health service; status
   `SERVING` CHỈ sau khi seed load xong (seed đọc từ env `SEED_PATH`, mặc định canonical path —
   xem application.yml). *P1 plan-critic:* verify `mvn dependency:tree | grep grpc-services` —
   grpc-services có thể chỉ là transitive của devh starter; thiếu → thêm ĐÚNG 1 dòng
   `io.grpc:grpc-services` vào pom (task riêng trong SF-2).
2. **Go batching-service** (`cmd/server/main.go`, port 50052): register `grpc_health_v1`
   (thư viện chuẩn `google.golang.org/grpc/health` — có sẵn trong go.mod grpc). Status SERVING
   sau khi service boot xong (seed đọc từ `CANONICAL_SEED_PATH`).
3. **Python print-service** (`print_service/server.py`, port 50053 hardcoded): serve
   `grpc_health.v1` (grpcio có sẵn `grpc_health.v1`). Status SERVING sau seed load
   (`PRINT_SERVICE_SEED_PATH`).
4. **nginx**: thêm `location /healthz { return 200 'ok'; access_log off; }` vào `docker/nginx.conf`.
   SF-2 sở hữu conf change; REBUILD web image là việc SF-4 (SF-2 verify bằng docker build ad hoc).
5. **grpcurl smoke script** (vd `scripts/grpc-health-smoke.sh`): gọi Check trên từng service
   (local binary run hoặc docker build ad hoc — KHÔNG tạo build script riêng; build entry point
   duy nhất là scripts/k8s-build-images.sh của SF-1).
6. **Unit tests** cho health logic mỗi service theo test convention hiện có của service đó.
7. **Constraint chống conflict FI-245:** chỉ THÊM file mới + registration dòng tối thiểu trong
   file boot/server hiện có — KHÔNG refactor, KHÔNG đụng business logic, KHÔNG đụng dial code Go
   (WithBlock giữ nguyên — ordering do SF-4 initContainer xử lý).

## Touch map (SF-2 sở hữu)

```
services/fulfillment-service/src/**/health/*        (CREATE — Java health service + config)
services/fulfillment-service/pom.xml                (EDIT — CHỈ nếu thiếu grpc-services, 1 dòng)
services/batching-service/internal/health/* hoặc tương đương (CREATE — Go)
services/print-service/**/health*.py                (CREATE — Python)
docker/nginx.conf                                   (EDIT — thêm location /healthz)
scripts/grpc-health-smoke.sh                        (CREATE)
tests theo convention từng service                  (CREATE)
services/batching-service/cmd/server/main.go        (EDIT TỐI THIỂU — register health only)
```
READ-ONLY: docker-compose.yml, Dockerfile.web, services/*/Dockerfile, apps/**, api/seed/**.

## ACCEPTANCE (user-visible)

- Chạy từng service local (cách hiện có — run.sh/boot-all.sh pattern) + `scripts/grpc-health-smoke.sh`
  → cả 3 gRPC services trả SERVING sau khi seed loaded; trước seed xong → NOT_SERVING (readiness thật).
- `curl localhost:3000/healthz` trên web image build ad hoc → 200 ok.
- Local dev flow cũ KHÔNG đổi: `scripts/boot-all.sh` vẫn boot được như trước (health là additive).

## Boundary (KHÔNG làm)

- KHÔNG viết k8s manifests / probe wiring (SF-4 chỉ tiêu thụ endpoint SF-2 tạo).
- KHÔNG sửa Dockerfiles (verify-unchanged là 1 task: `git diff` trên 5 Dockerfile phải rỗng).
- KHÔNG sửa Go dial/WithBlock code (SF-4 initContainer lo ordering).
- KHÔNG tạo image build script (SF-1 sở hữu duy nhất).
- Keycloak, ingress, deploy script → SF-3/SF-4.
