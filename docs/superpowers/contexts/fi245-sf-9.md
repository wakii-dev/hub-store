# FI-245 SF-9 Context Pack — Dashboard thống kê
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.9. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 2 — deps SF-2 Done (orders trên Postgres).

## Spec slice
1. Aggregate API (BE owner SF-9): 1 endpoint (hoặc nhóm nhỏ) trả đủ: orders-per-day 30 ngày, completion/cancel rate, workload per delivery staff, counts theo status (chờ xử lý/đang giao). SQL aggregate GROUP BY — KHÔNG N+1, KHÔNG load toàn bộ rows về BFF để đếm.
2. FE màn Dashboard: 4-5 khối thống kê + chart (antd4 charts — @ant-design/charts hoặc antd Progress/Statistic đơn giản; ưu tiên ít deps mới, pin version).
3. Đường dẫn mặc định sau login: Manager → Dashboard; Coordinator/WarehouseOps giữ firstPathForRole hiện tại.
4. Dữ liệu thật từ Postgres (fulfillment DB) — E2E seed phải cho ra số liệu assert được (đếm từ canonical-seed.json).
5. UI theo sf6-direction nếu có; chưa có → antd4 sạch, SF-11 hội tụ.

## Touch map (SF-9 sở hữu)
```
services/fulfillment-service/  (aggregate RPC/query — method MỚI, không đổi message cũ)
services/bff-gateway/          (REST /stats hoặc /dashboard)
apps/shell/src/ + orders-mf    (dashboard screen — đặt shell hoặc orders-mf tùy cấu trúc nav)
e2e/xx-dashboard.spec.ts       (mới)
```
READ-ONLY: batching (KHÔNG đụng — dashboard chỉ orders), seed JSON, compose.

## ACCEPTANCE (user-visible)
- Login Manager → Dashboard hiện số liệu khớp seed (đếm tay được từ canonical-seed.json).
- Charts render (không màn trắng), responsive vừa màn hình laptop.
- Tạo thêm đơn → dashboard cập nhật sau refetch (không cần SSE — đó là SF-10).

## Boundary (KHÔNG làm)
- KHÔNG realtime (SF-10).
- KHÔNG custom báo cáo cấu hình được / date-picker tùy chọn — cố định 30 ngày + hôm nay (đủ dùng, SF sau mở rộng).
- KHÔNG đụng batching/batches stats (sau).
