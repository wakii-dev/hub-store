# FI-245 SF-5 Context Pack — Convergence: production compose + E2E + deploy docs
> Đọc file này THAY VÌ tự tổng hợp. Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Đây là SF CUỐI (tier 2) — deps SF-2+SF-3+SF-4 Done.

## Spec slice (SF-5 chịu trách nhiệm)
1. **Compose full-chain verify (trước tiên)**: `docker compose up` chain postgres → orders-migrate + batches-migrate (one-shot) → db-seed (completed) → apps (`service_completed_successfully`) → `psql` thấy ORD-3001 + BATCH-0001 qua compose up (acceptance mà SF-1 tier-0 không test được — wiring SF-1 authored, chain verify ở đây).
2. **Compose full build**: `docker compose up --build` từ repo sạch → postgres + keycloak + db-seed + 3 services + bff + web lên đủ, login thật, full flow D1→D3 chạy được.
3. **Persistence proof (bắt buộc, evidence)**: tạo 1 phiếu → `docker compose restart` (giữ volume) → đăng nhập lại → phiếu còn; `psql` SELECT thấy rows mutations.
4. **E2E 13/13 xanh** với `E2E=1`: boot-all.sh gọi `scripts/reset-db.sh` (SF-1 util) + reset keycloak volume trước boot; specs chạy qua auth thật (helper SF-4).
5. **boot-all.sh update**: dùng `scripts/wait-db.sh` (SF-1) + wait keycloak ready; `E2E=1` → reset trước boot.
6. **README deploy guide**: compose up từ đầu, tạo/đổi user Keycloak (reset volume), biến env bắt buộc (contract `.env.example` SF-1), backup `pg_dump` cả 2 DB, reset DB khi seed đổi.
7. **Security re-check trên diff tổng story**: M-2 resolved pattern, H-1 residue note, M-3 note. Dispatch security-audit + code-reviewer + verifier per protocol.

## Touch map (SF-5 sở hữu)
```
scripts/boot-all.sh          (wait DB/keycloak + E2E=1 reset)
README.md                    (deploy guide + backup)
e2e/playwright.config.ts     (chỉ nếu cần webServer wiring cho E2E=1 — specs KHÔNG đổi)
docs/superpowers/…           (plan file)
```
READ-ONLY: toàn bộ services/**, apps/**, docker/, scripts/seed-db.sh + reset-db.sh (SF-1), migration files (SF-2/SF-3), realm JSON (SF-4). Convergence fix bug được PHÉP nhưng: fix đụng ownership SF cũ → ghi rõ trong audit comment (không refactor).

## ACCEPTANCE (user-visible)
1. Máy sạch + `docker compose up --build` → :3000 → login thật → D1 lọc → D1b tạo phiếu (DnD+suggest+shipper) → D2 hủy/hoàn tất → D3 in PDF: toàn bộ OK.
2. Tạo phiếu → restart → phiếu VẪN ĐÓ sau login lại.
3. E2E Playwright xanh với E2E=1.
4. `run.sh` từng service chạy khi postgres lên; unit test pass không cần DB.
5. README đủ để người mới deploy được.

## Boundary (KHÔNG làm)
- KHÔNG TLS/HA/monitoring/k8s; KHÔNG đổi proto/REST shape; KHÔNG mTLS s2s; KHÔNG đổi seed JSON.
- KHÔNG refactor code SF khác — chỉ fix bug convergence-specific.
