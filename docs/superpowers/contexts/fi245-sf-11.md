# FI-245 SF-11 Context Pack — FE convergence mới: audit viewer + export UI + mobile
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.11. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 3 — deps SF-6 (design system) + SF-7 + SF-8 + SF-9 + SF-10 Done. Đây là SF HỘI TỤ của nửa Product completion.

## Spec slice
1. **Audit-log viewer** (Manager): màn list activity log (SF-7 API) — filter actor/action/date, bảng phân trang, theo ĐÚNG design system SF-6.
2. **Export UI**: nút "Export CSV" trên D1 orders (gần filter bar) → tải file theo filter hiện tại; loading state + empty-result handling.
3. **Mobile responsive polish**: breakpoints tablet (shipper ra kho) — D1/D2/D3 + dashboard + users usable ở ~768px; nav collapse; bảng → card hoặc scroll ngang có kiểm soát.
4. **Design harmonize**: screens SF-8 (Users) + SF-9 (Dashboard) được build trước direction (nếu có) → ở đây REVIEWS + sửa cho đúng 100% design system SF-6 (tokens, spacing, components).
5. Skeletons + empty-states cho tất cả screens mới.
6. **E2E specs mới xanh**: users, dashboard, export, audit, realtime (specs SF-8/9/10 đã viết) — cộng 13 specs cũ VẪN xanh KHÔNG sửa.

## Touch map (SF-11 sở hữu)
```
apps/shell/src/                  (audit viewer screen + nav)
apps/orders-mf/src/              (export button + mobile polish D1/D1b)
apps/fulfillment-mf/src/         (mobile polish D2)
apps/print-mf/src/               (mobile polish D3)
e2e/                             (chỉ specs MỚI nếu còn thiếu)
```
READ-ONLY: services/** (nếu phát hiện bug BE → REQUIREMENT-GAP/comment, không tự sửa ngoài convergence-fix có audit), compose, realm JSON.

## ACCEPTANCE (user-visible)
- Manager thấy màn Audit log đẹp, lọc được, phân trang.
- Nút Export trên D1 tải đúng file theo filter đang chọn.
- Mở app trên tablet/resize ~768px: mọi màn dùng được, không vỡ layout.
- TẤT CẢ screens (cũ + mới) cùng 1 design system SF-6 — browser 3 tầng verify.
- E2E: 13 cũ + specs mới = toàn xanh.

## Boundary (KHÔNG làm)
- KHÔNG đổi business logic/API shape.
- KHÔNG mobile native/PWA — chỉ responsive web.
- KHÔNG sửa specs E2E cũ.
