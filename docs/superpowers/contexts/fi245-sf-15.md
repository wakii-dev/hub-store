# FI-245 SF-15 Context Pack — NVC backend: Ahamove THẬT (KHÔNG MOCK)
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.15. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 3 — deps SF-3 (batches trên Postgres).

## Spec slice
1. **Tích hợp API Ahamove THẬT** — `api.ahamove.com` partner API: client (Go, batching-service) với auth/signing qua env `AHAMOVE_API_KEY` + `AHAMOVE_PARTNER_TOKEN`. **CẤM mock server / provider giả / fixture response trong runtime path.** E2E test NVC chỉ chạy khi env có credential (skip-if-no-env, KHÔNG fake data).
2. **Endpoints** (qua BFF REST, shape khớp app gốc): `POST /delivery-batch/quotes`, `POST /delivery-batch/planning/confirm`, `POST /delivery-batch/booking` (batchCode + shipmentPlannings[] COD/totalBill/stopOrder), `POST /delivery-batch/cancel-delivery-order`, `POST /delivery-batch/cancel-batch`, `GET /delivery-batch/searchbookingdetail?planningIds=`.
3. **Batching DB V2** (golang-migrate): plannings, bookings, shipment_statuses/tracking_events, addon_services catalog, fee_limits (per-shop).
4. **Fee-limit rules BE-authoritative**: quote trả `isExceedFeeLimit`; confirm/booking chặn khi total > limit — FE chỉ render, BE là chốt chặn.
5. Addon catalog: nhóm ROUTE/LOADING (radio), DOCUMENT (checkbox, phí 0), ROUND-TRIP; filter theo xe.
6. **Credential**: nếu user chưa cấp `AHAMOVE_*` → post REQUIREMENT-GAP lên FI-245 + làm hết phần không blocked (schema, endpoints wiring, fee rules — gọi Ahamove thật khi có key).

## Touch map (SF-15 sở hữu)
```
services/batching-service/        (Ahamove client + endpoints logic + migrations V2)
services/bff-gateway/             (REST proxy routes /delivery-batch/*)
.env / .env.example               (AHAMOVE_* vars)
e2e/xx-nvc-api.spec.ts            (mới — skip-if-no-env)
```
READ-ONLY: fulfillment (KHÔNG đụng), apps/** (SF-16), compose (KHÔNG thêm service giả), realm, seed.

## ACCEPTANCE (user-visible)
- Với credential thật: quotes theo batch → phí thật theo tải trọng + distance; book → vận đơn thật trên Ahamove; tracking timeline thật; hủy thật.
- Vượt hạn mức phí → BE chặn booking.
- Không credential → endpoints wiring + schema + tests (skip NVC-call tests) hoàn tất, REQUIREMENT-GAP registered.

## Boundary (KHÔNG làm)
- KHÔNG FE (SF-16). KHÔNG mock/fixture trong runtime path — tuyệt đối.
- KHÔNG đổi batching flow cũ. KHÔNG đụng fulfillment DB.
