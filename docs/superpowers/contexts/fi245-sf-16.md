# FI-245 SF-16 Context Pack — NVC FE: carrier section + replan/rebook/tracking
> Epic spec: docs/superpowers/specs/ict-service-support-postgres-prod-spec.md §3.16. Bracket: docs/superpowers/brackets/fi245-postgres-production.md. Tier 4 — deps SF-15 (API) + SF-6 (design system).

## Spec slice
1. **D1b carrier section**: 3 nhóm carrier — Tự giao (KHO_CN) / xe tải quotes (MockAhamove: 6 tải trọng, phí, icon, isExceedFeeLimit → disable radio) / FPT_DELIVERY; recalculate khi đổi xe/addon; **fee-limit gates** (baseFee > limit → disable; total > limit → block submit + message).
2. **Addon selector**: ROUTE/LOADING radio, DOCUMENT checkbox, ROUND-TRIP; filter theo xe (serviceApplieds); unavailable → disable.
3. **Replan/rebook** từ D2: mode `replan` (đơn thiếu hàng isShortaged loại; gate CANCELLED/MRSA_PENDING_REPLAN) + `rebook` (gate book-hủy/lỗi) — reuse CreateBatchingModal mode mới.
4. **Hủy vận đơn**: per-đơn hoặc cả batch + note tự động; hiển thị theo flag BE allowCancelDelivery.
5. **Tracking modal**: timeline 2 cột (BE timeline + partner timeline) + link urltracking; master mapping 15 trạng thái vận đơn (ORDER_CREATED → Đang tìm tài xế … DRIVER_REASSIGNING).
6. UI theo 100% design system SF-6; KHÔNG đổi testid/DOM screens cũ; E2E cũ xanh.

## Touch map (SF-16 sở hữu)
```
apps/orders-mf/src/    (D1b carrier section + addon + fee gates)
apps/fulfillment-mf/src/ (replan/rebook/cancel/tracking từ D2)
apps/shell/src/        (shared tracking modal nếu cần)
e2e/xx-nvc-fe.spec.ts  (mới)
```
READ-ONLY: services/** (bug BE → REQUIREMENT-GAP lên epic), compose, realm.

## ACCEPTANCE (user-visible)
- Tạo phiếu: thấy quotes 6 xe, chọn xe + addon → phí tổng cập nhật; vượt hạn mức → bị chặn với message rõ.
- Book xong → D2 thấy vận đơn + tài xế; mở tracking → timeline chạy; hủy → book lại được.
- Replan phiếu có đơn thiếu hàng → đơn thiếu bị loại đúng.
- E2E cũ + mới xanh.

## Boundary (KHÔNG làm)
- KHÔNG sửa logic API SF-15 (bug → REQUIREMENT-GAP).
- KHÔNG bản đồ trực quan (map view — gốc cũng không có).
- KHÔNG đổi business flow batch cũ.
