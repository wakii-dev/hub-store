apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  clusterIP: None   # headless — client nối thẳng postgres-0 (single instance dev)
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      name: pg
