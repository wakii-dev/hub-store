# SF-4: print-service (Python gRPC :50053) — thay placeholder SF-1.
# KHÔNG sửa k8s/base/kustomization.yaml (composition contract).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hub-store

resources:
  - deployment.yaml
  - service.yaml
