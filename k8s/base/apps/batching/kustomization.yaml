# SF-4: batching-service (Go gRPC :50052) — thay placeholder SF-1.
# KHÔNG sửa k8s/base/kustomization.yaml (composition contract).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hub-store

resources:
  - deployment.yaml
  - service.yaml
