node_modules/
dist/
.turbo/
*.tsbuildinfo
coverage/
# SF-12 — secrets local-only, KHÔNG track (mẫu: .env.example → cp → điền local)
.env
.env.local
.env.*.local
.vite/
*.log
# Generated by @module-federation/dts-plugin khi build remotes (type declarations
# cho workspace deps bị exposes). Build artifacts — type resolution vẫn đi qua .ts nguồn.
packages/*/src/**/*.d.ts
# MF runtime diagnostics (thay đổi mỗi lần build/dev)
.mf/

# SF-11 E2E artifacts (Playwright)
e2e/test-results/
e2e/playwright-report/
# SF-4 — storageState do auth.setup tạo mỗi lần chạy (session Keycloak dev)
e2e/.auth/
# SF-21 — batchCode state giữa các test E2E (spec 08, sống qua run mới)
e2e/.sf21-batch-code
__pycache__/

# Claude Code local settings
.claude/
