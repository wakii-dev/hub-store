// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.7.7
//   protoc               v5.29.3
// source: hubstore/print/v1/print.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import {
  type CallOptions,
  type ChannelCredentials,
  Client,
  type ClientOptions,
  type ClientUnaryCall,
  type handleUnaryCall,
  makeGenericClientConstructor,
  type Metadata,
  type ServiceError,
  type UntypedServiceImplementation,
} from "@grpc/grpc-js";

export const protobufPackage = "hubstore.print.v1";

/**
 * PrintType — 5 loại phiếu in (D3 tabs, REQUIREMENTS §3 D3).
 * Wire values map sang packages/shared PRINT_TYPES strings:
 * 1 bill · 2 delivery · 3 handover_receipt · 4 goods_handover · 5 installation_acceptance.
 * Proto enums cần giá trị 0 (buf ENUM_FIRST_VALUE_ZERO) → UNSPECIFIED = 0,
 * không dùng trên wire (BFF reject request có UNSPECIFIED).
 */
export enum PrintType {
  PRINT_TYPE_UNSPECIFIED = 0,
  /** PRINT_TYPE_BILL - Biên bản / phiếu thu COD */
  PRINT_TYPE_BILL = 1,
  /** PRINT_TYPE_DELIVERY - Vận đơn / label giao hàng */
  PRINT_TYPE_DELIVERY = 2,
  /** PRINT_TYPE_HANDOVER_RECEIPT - Bàn giao shipper */
  PRINT_TYPE_HANDOVER_RECEIPT = 3,
  /** PRINT_TYPE_GOODS_HANDOVER - Bàn giao kho */
  PRINT_TYPE_GOODS_HANDOVER = 4,
  /** PRINT_TYPE_INSTALLATION_ACCEPTANCE - Nghiệm thu lắp đặt */
  PRINT_TYPE_INSTALLATION_ACCEPTANCE = 5,
  UNRECOGNIZED = -1,
}

export function printTypeFromJSON(object: any): PrintType {
  switch (object) {
    case 0:
    case "PRINT_TYPE_UNSPECIFIED":
      return PrintType.PRINT_TYPE_UNSPECIFIED;
    case 1:
    case "PRINT_TYPE_BILL":
      return PrintType.PRINT_TYPE_BILL;
    case 2:
    case "PRINT_TYPE_DELIVERY":
      return PrintType.PRINT_TYPE_DELIVERY;
    case 3:
    case "PRINT_TYPE_HANDOVER_RECEIPT":
      return PrintType.PRINT_TYPE_HANDOVER_RECEIPT;
    case 4:
    case "PRINT_TYPE_GOODS_HANDOVER":
      return PrintType.PRINT_TYPE_GOODS_HANDOVER;
    case 5:
    case "PRINT_TYPE_INSTALLATION_ACCEPTANCE":
      return PrintType.PRINT_TYPE_INSTALLATION_ACCEPTANCE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return PrintType.UNRECOGNIZED;
  }
}

export function printTypeToJSON(object: PrintType): string {
  switch (object) {
    case PrintType.PRINT_TYPE_UNSPECIFIED:
      return "PRINT_TYPE_UNSPECIFIED";
    case PrintType.PRINT_TYPE_BILL:
      return "PRINT_TYPE_BILL";
    case PrintType.PRINT_TYPE_DELIVERY:
      return "PRINT_TYPE_DELIVERY";
    case PrintType.PRINT_TYPE_HANDOVER_RECEIPT:
      return "PRINT_TYPE_HANDOVER_RECEIPT";
    case PrintType.PRINT_TYPE_GOODS_HANDOVER:
      return "PRINT_TYPE_GOODS_HANDOVER";
    case PrintType.PRINT_TYPE_INSTALLATION_ACCEPTANCE:
      return "PRINT_TYPE_INSTALLATION_ACCEPTANCE";
    case PrintType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Printer — máy in theo shopCode (canonical seed, SF-2 Task 4). */
export interface Printer {
  id: string;
  name: string;
  shopCode: string;
}

export interface ListPrintersRequest {
  shopCode: string;
}

export interface ListPrintersResponse {
  printers: Printer[];
}

export interface PrintRequest {
  /**
   * Fat payload: Batch (spec §3.4) do BFF hydrate từ batching-service rồi
   * serialize (canonical JSON) — Python KHÔNG gọi Go (spec §3.2 P1 pin).
   */
  batchPayload: Uint8Array;
  printType: PrintType;
  printerId: string;
}

export interface PrintResponse {
  /** PDF bytes (reportlab render, stateless). */
  pdfContent: Uint8Array;
}

function createBasePrinter(): Printer {
  return { id: "", name: "", shopCode: "" };
}

export const Printer: MessageFns<Printer> = {
  encode(message: Printer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.shopCode !== "") {
      writer.uint32(26).string(message.shopCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Printer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrinter();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.shopCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Printer {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      shopCode: isSet(object.shopCode) ? globalThis.String(object.shopCode) : "",
    };
  },

  toJSON(message: Printer): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.shopCode !== "") {
      obj.shopCode = message.shopCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Printer>, I>>(base?: I): Printer {
    return Printer.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Printer>, I>>(object: I): Printer {
    const message = createBasePrinter();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.shopCode = object.shopCode ?? "";
    return message;
  },
};

function createBaseListPrintersRequest(): ListPrintersRequest {
  return { shopCode: "" };
}

export const ListPrintersRequest: MessageFns<ListPrintersRequest> = {
  encode(message: ListPrintersRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopCode !== "") {
      writer.uint32(10).string(message.shopCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPrintersRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPrintersRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopCode = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPrintersRequest {
    return { shopCode: isSet(object.shopCode) ? globalThis.String(object.shopCode) : "" };
  },

  toJSON(message: ListPrintersRequest): unknown {
    const obj: any = {};
    if (message.shopCode !== "") {
      obj.shopCode = message.shopCode;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPrintersRequest>, I>>(base?: I): ListPrintersRequest {
    return ListPrintersRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPrintersRequest>, I>>(object: I): ListPrintersRequest {
    const message = createBaseListPrintersRequest();
    message.shopCode = object.shopCode ?? "";
    return message;
  },
};

function createBaseListPrintersResponse(): ListPrintersResponse {
  return { printers: [] };
}

export const ListPrintersResponse: MessageFns<ListPrintersResponse> = {
  encode(message: ListPrintersResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.printers) {
      Printer.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPrintersResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPrintersResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.printers.push(Printer.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPrintersResponse {
    return {
      printers: globalThis.Array.isArray(object?.printers) ? object.printers.map((e: any) => Printer.fromJSON(e)) : [],
    };
  },

  toJSON(message: ListPrintersResponse): unknown {
    const obj: any = {};
    if (message.printers?.length) {
      obj.printers = message.printers.map((e) => Printer.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPrintersResponse>, I>>(base?: I): ListPrintersResponse {
    return ListPrintersResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPrintersResponse>, I>>(object: I): ListPrintersResponse {
    const message = createBaseListPrintersResponse();
    message.printers = object.printers?.map((e) => Printer.fromPartial(e)) || [];
    return message;
  },
};

function createBasePrintRequest(): PrintRequest {
  return { batchPayload: new Uint8Array(0), printType: 0, printerId: "" };
}

export const PrintRequest: MessageFns<PrintRequest> = {
  encode(message: PrintRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchPayload.length !== 0) {
      writer.uint32(10).bytes(message.batchPayload);
    }
    if (message.printType !== 0) {
      writer.uint32(16).int32(message.printType);
    }
    if (message.printerId !== "") {
      writer.uint32(26).string(message.printerId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrintRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrintRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.batchPayload = reader.bytes();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.printType = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.printerId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrintRequest {
    return {
      batchPayload: isSet(object.batchPayload) ? bytesFromBase64(object.batchPayload) : new Uint8Array(0),
      printType: isSet(object.printType) ? printTypeFromJSON(object.printType) : 0,
      printerId: isSet(object.printerId) ? globalThis.String(object.printerId) : "",
    };
  },

  toJSON(message: PrintRequest): unknown {
    const obj: any = {};
    if (message.batchPayload.length !== 0) {
      obj.batchPayload = base64FromBytes(message.batchPayload);
    }
    if (message.printType !== 0) {
      obj.printType = printTypeToJSON(message.printType);
    }
    if (message.printerId !== "") {
      obj.printerId = message.printerId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrintRequest>, I>>(base?: I): PrintRequest {
    return PrintRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrintRequest>, I>>(object: I): PrintRequest {
    const message = createBasePrintRequest();
    message.batchPayload = object.batchPayload ?? new Uint8Array(0);
    message.printType = object.printType ?? 0;
    message.printerId = object.printerId ?? "";
    return message;
  },
};

function createBasePrintResponse(): PrintResponse {
  return { pdfContent: new Uint8Array(0) };
}

export const PrintResponse: MessageFns<PrintResponse> = {
  encode(message: PrintResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pdfContent.length !== 0) {
      writer.uint32(10).bytes(message.pdfContent);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PrintResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePrintResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.pdfContent = reader.bytes();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PrintResponse {
    return { pdfContent: isSet(object.pdfContent) ? bytesFromBase64(object.pdfContent) : new Uint8Array(0) };
  },

  toJSON(message: PrintResponse): unknown {
    const obj: any = {};
    if (message.pdfContent.length !== 0) {
      obj.pdfContent = base64FromBytes(message.pdfContent);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PrintResponse>, I>>(base?: I): PrintResponse {
    return PrintResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PrintResponse>, I>>(object: I): PrintResponse {
    const message = createBasePrintResponse();
    message.pdfContent = object.pdfContent ?? new Uint8Array(0);
    return message;
  },
};

export type PrintServiceService = typeof PrintServiceService;
export const PrintServiceService = {
  /** GET /fulfillment/print/printers?shopCode= — BFF lọc theo shop. */
  listPrinters: {
    path: "/hubstore.print.v1.PrintService/ListPrinters",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: ListPrintersRequest): Buffer => Buffer.from(ListPrintersRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): ListPrintersRequest => ListPrintersRequest.decode(value),
    responseSerialize: (value: ListPrintersResponse): Buffer =>
      Buffer.from(ListPrintersResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): ListPrintersResponse => ListPrintersResponse.decode(value),
  },
  /** POST /fulfillment/print — trả PDF bytes (application/pdf) qua BFF stream. */
  print: {
    path: "/hubstore.print.v1.PrintService/Print",
    requestStream: false,
    responseStream: false,
    requestSerialize: (value: PrintRequest): Buffer => Buffer.from(PrintRequest.encode(value).finish()),
    requestDeserialize: (value: Buffer): PrintRequest => PrintRequest.decode(value),
    responseSerialize: (value: PrintResponse): Buffer => Buffer.from(PrintResponse.encode(value).finish()),
    responseDeserialize: (value: Buffer): PrintResponse => PrintResponse.decode(value),
  },
} as const;

export interface PrintServiceServer extends UntypedServiceImplementation {
  /** GET /fulfillment/print/printers?shopCode= — BFF lọc theo shop. */
  listPrinters: handleUnaryCall<ListPrintersRequest, ListPrintersResponse>;
  /** POST /fulfillment/print — trả PDF bytes (application/pdf) qua BFF stream. */
  print: handleUnaryCall<PrintRequest, PrintResponse>;
}

export interface PrintServiceClient extends Client {
  /** GET /fulfillment/print/printers?shopCode= — BFF lọc theo shop. */
  listPrinters(
    request: ListPrintersRequest,
    callback: (error: ServiceError | null, response: ListPrintersResponse) => void,
  ): ClientUnaryCall;
  listPrinters(
    request: ListPrintersRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: ListPrintersResponse) => void,
  ): ClientUnaryCall;
  listPrinters(
    request: ListPrintersRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: ListPrintersResponse) => void,
  ): ClientUnaryCall;
  /** POST /fulfillment/print — trả PDF bytes (application/pdf) qua BFF stream. */
  print(
    request: PrintRequest,
    callback: (error: ServiceError | null, response: PrintResponse) => void,
  ): ClientUnaryCall;
  print(
    request: PrintRequest,
    metadata: Metadata,
    callback: (error: ServiceError | null, response: PrintResponse) => void,
  ): ClientUnaryCall;
  print(
    request: PrintRequest,
    metadata: Metadata,
    options: Partial<CallOptions>,
    callback: (error: ServiceError | null, response: PrintResponse) => void,
  ): ClientUnaryCall;
}

export const PrintServiceClient = makeGenericClientConstructor(
  PrintServiceService,
  "hubstore.print.v1.PrintService",
) as unknown as {
  new (address: string, credentials: ChannelCredentials, options?: Partial<ClientOptions>): PrintServiceClient;
  service: typeof PrintServiceService;
  serviceName: string;
};

function bytesFromBase64(b64: string): Uint8Array {
  if ((globalThis as any).Buffer) {
    return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
  } else {
    const bin = globalThis.atob(b64);
    const arr = new Uint8Array(bin.length);
    for (let i = 0; i < bin.length; ++i) {
      arr[i] = bin.charCodeAt(i);
    }
    return arr;
  }
}

function base64FromBytes(arr: Uint8Array): string {
  if ((globalThis as any).Buffer) {
    return globalThis.Buffer.from(arr).toString("base64");
  } else {
    const bin: string[] = [];
    arr.forEach((byte) => {
      bin.push(globalThis.String.fromCharCode(byte));
    });
    return globalThis.btoa(bin.join(""));
  }
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
